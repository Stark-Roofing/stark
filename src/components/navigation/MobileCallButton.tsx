/**
 * MobileCallButton — disabled 2026-06-08 per client request to remove all
 * call CTAs from the site. Kept as no-op so existing importers don't break.
 */
const MobileCallButton = (_props: { onClick?: () => void } = {}) => null;

export default MobileCallButton;
